services:
  - type: web
    name: certa-hora
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    healthCheckPath: /api/me
    envVars:
      - key: PYTHON_VERSION
        value: 3.13.4
